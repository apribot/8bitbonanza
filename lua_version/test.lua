local samples = 100000
local rate = 44100
local bits = 8
local channels = 1

--soundData = love.sound.newSoundData( samples, rate, bits, channels )

--src1:setVolume(0.9) -- 90% of ordinary volume
--src1:setPitch(0.5) -- one octave lower
--src2:setVolume(0.7)
print('creating samples')
--for t=1,10000 do
--	data:setSample(t, (((  t%255  )/127.5)-1))
--end
print('done')
--soundData:play()